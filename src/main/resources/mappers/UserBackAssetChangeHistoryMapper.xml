<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fzm.mall.mapper.UserBackAssetChangeHistoryMapper">
    <insert id="insert">
        insert into user_back_asset_change_history(address,
                                                   coin_type,
                                                   number,
                                                   balance_type,
                                                   log_type,
                                                   withdraw_type,
                                                   create_time,
                                                   extend_data)
        VALUES (#{address},
                #{coinType},
                #{number},
                #{balanceType},
                #{logType},
                #{withdrawType},
                #{createTime},
                #{extendData})
    </insert>

    <update id="updateLogTypeByLogId">
        update user_back_asset_change_history
        set log_type = #{toLogType}
        <if test="extendData != null">
            , extend_data = #{extendData}
        </if>
        where log_id = #{logId}
        and log_type = #{fromLogType}
    </update>

    <select id="listByPageQO" resultType="com.fzm.mall.entity.dataobject.UserBackAssetChangeHistoryDO">
        select *
        from user_back_asset_change_history
        <where>
            <if test="address != null and address != ''">
                and address = #{address}
            </if>
            <if test="coinType != null and coinType >= 0">
                and coin_type = #{coinType}
            </if>
            <if test="logType != null and logType >= 0">
                and log_type = #{logType}
            </if>
            <if test="withdrawType != null and withdrawType >= 0">
                and withdraw_type = #{withdrawType}
            </if>
            <if test="startTime != null and startTime > 0">
                and create_time >= #{startTime}
            </if>
            <if test="endTime != null and endTime > 0">
                and #{endTime} >= create_time
            </if>
            <if test="vague != null and vague != ''">
                and extend_data like CONCAT('%', #{vague}, '%')
            </if>
        </where>
        order by log_id desc
    </select>

    <select id="listByLogType" resultType="com.fzm.mall.entity.dataobject.UserBackAssetChangeHistoryDO">
        select *
        from user_back_asset_change_history
        where log_type = #{logType}
    </select>

</mapper>