<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fzm.mall.mapper.GoodsFavoriteMapper">

    <insert id="insert">
        insert into goods_favorite(goods_id, address, create_time)
        VALUES (#{goodsId}, #{address}, #{createTime})
    </insert>

    <delete id="delByGoodsIdAddress">
        delete
        from goods_favorite
        where goods_id = #{goodsId}
          and address = #{address}
    </delete>

    <select id="getByGoodsIdAddress" resultType="com.fzm.mall.entity.dataobject.GoodsFavoriteDO">
        select *
        from goods_favorite
        where goods_id = #{goodsId}
          and address = #{address}
    </select>
</mapper>